
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <div class="alert alert-warning alert-dismissible fade show" role="alert">
      <strong>Esta página web esta actualmente en desarrollo.</strong> versión: 0.0.1 con responsable: Alejandro Galán, agalan3@alu.ucam.edu.
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    <title>MED-TOOLS - Iniciar sesión</title>

    
    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
  
          <div class="card">
            <div class="card-header">
              <h4 class="text-center">Cita online con el doctor</h4>
            </div>
  
            <div class="card-body">

                <p>Para poder acceder a la cita introduzca en el siguiente campo el código de reunión que le ha facilitado su doctor por correo</p>
                <form>
                    <input type="text" id="codigo_reunion" name="codigo_reunion" placeholder="Código de reunión">
                    <button id="boton_reunion" class="btn btn-primary" type="button" disabled>Ir a la reunión</button>
                  </form>
                  <br>
                <a href='/login' class="btn btn-primary btn-block">Volver</a>

            </div>
          </div>
  
        </div>
      </div>
    </div>

    

    <script> //CITA ONLINE
        const codigoReunion = document.getElementById("codigo_reunion");
        const botonReunion = document.getElementById("boton_reunion");
      
        codigoReunion.addEventListener("input", function() {
          if (codigoReunion.value.trim() === "") {
            botonReunion.disabled = true;
          } else {
            botonReunion.disabled = false;
          }
        });
      
        botonReunion.addEventListener("click", function() {
          const urlReunion = "https://meet.google.com/" + codigoReunion.value;
          window.location.href = urlReunion;
        });
      </script>

    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.min.js"></script>
  </body>
</html>
